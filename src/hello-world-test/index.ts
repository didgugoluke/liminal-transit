/**
 * Simple Hello World Test - Copilot E2E Validation
 * 
 * Minimal test case to validate the complete AI agent pipeline
 * from issue creation through automated merge.
 */

export class HelloWorld {
  /**
   * Returns a simple greeting message
   * @returns {string} The greeting 'Hello, World!'
   */
  greet(): string {
    return 'Hello, World!';
  }
}

export default HelloWorld;